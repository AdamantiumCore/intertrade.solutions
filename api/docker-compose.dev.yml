services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: intertrade
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - ${DATABASE_PORT}:3306